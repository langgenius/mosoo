---
import "../styles/global.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

interface Props {
  title: string;
  description: string;
  canonical?: string;
  ogImage?: string;
  type?: "website" | "article";
  publishedTime?: Date;
  author?: string;
}
const {
  title,
  description,
  canonical,
  ogImage,
  type = "website",
  publishedTime,
  author,
} = Astro.props;
// Drop any trailing slash so the canonical matches the extensionless URL the
// user types and the host actually serves.
const cleanPath = Astro.url.pathname.replace(/\/$/, "") || "/";
const canonicalUrl = canonical ?? new URL(cleanPath, Astro.site).href;
const ogImageUrl = ogImage ?? new URL("/og-default.png", Astro.site).href;
---

<!doctype html>
<html lang="en" data-theme="landing">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <link rel="canonical" href={canonicalUrl} />
    <link rel="icon" type="image/svg+xml" href="/blog/favicon.svg" />
    <link
      rel="alternate"
      type="application/rss+xml"
      title="Mosoo Blog"
      href="/blog/rss.xml"
    />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta property="og:type" content={type} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:site_name" content="Mosoo" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />
    {
      type === "article" && publishedTime && (
        <meta property="article:published_time" content={publishedTime.toISOString()} />
      )
    }
    {type === "article" && author && <meta property="article:author" content={author} />}
    <slot name="head" />
  </head>
  <body class="min-h-screen bg-[color:var(--bg)] text-[color:var(--fg-1)]">
    <Header />
    <slot />
    <Footer />
  </body>
</html>
