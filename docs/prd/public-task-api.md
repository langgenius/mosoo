# Public Task API — superseded

Status: superseded.

This file is retained only so old links explain why the task-shaped API and
`POST /threads/{threadId}/files` route disappeared. Neither is a current Mosoo
contract and neither requires compatibility.

Use [Public Thread API Surface](./public-thread-api-surface.md) for product
semantics and `GET /api/v1/openapi.json` for the exact HTTP contract.
