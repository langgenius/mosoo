# Changelog

Every `0.x` breaking change will be called out here.

## 0.1.0-beta.0

- First Public Beta of the self-contained `@mosoo/sdk` package.
- Added recoverable Thread/Run task creation, typed terminal results and canonical final output.
- Added file upload, typed Run artifact references, live event access and delegation-token verification for trusted backends.
- Added Node.js 22/24 LTS and Cloudflare Workers package/runtime checks.
